apiVersion: v1
kind: Service
metadata:
  name: {{ include "afristocks.fullname" . }}
  labels:
    {{- include "afristocks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "afristocks.selectorLabels" . | nindent 4 }}
